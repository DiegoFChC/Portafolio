<div class='bg' />
<div class='bg-rings'>
	<img src='/rings.png' alt='rings-bg'>
</div>
<div class='bg-icons'>
	<img src='/icons.png' class='parallax' data-speed='2' alt='icons-bg'>
</div>

<script>
  document.addEventListener('mousemove', (e) => {
    const x = e.clientX / window.innerWidth - 0.5
    const y = e.clientY / window.innerHeight - 0.5

    document.querySelectorAll('.parallax').forEach((item) => {
      const speed = item.getAttribute('data-speed')
      item.style.transform = `translate(${x * speed * 20}px, ${y * speed * 20}px)`
    })
  })
</script>

<style>
  .bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100vh;
		/* background: radial-gradient(circle at center top, rgba(128, 0, 128, 0.267) 10%, rgb(3, 0, 10) 70%); */
		background: radial-gradient(circle at center 0, rgba(128, 0, 128, 0.267) 10%, rgb(3, 0, 10) 70%);
		z-index: -1000;
	}

	.bg-rings {
		width: 100%;
		max-height: 100vh;
		position: absolute;
		top: -100px;
		display: flex;
		justify-content: center;
		overflow: hidden;
		z-index: -999;
	}

	.bg-rings img {
		width: 70%;
		object-fit: contain;
		opacity: 0.4;
		mask-image: linear-gradient(to top, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 100%);
	}

  .bg-icons {
    box-sizing: border-box;
    padding: 0 70px;
    width: 100%;
		max-height: 100vh;
		position: absolute;
		top: 50px;
		display: flex;
		justify-content: center;
		overflow: hidden;
		z-index: -998;
  }

  .bg-icons img {
    width: 100%;
    opacity: 0.4;
		object-fit: cover;
  }

  .parallax {
    transition: transform 1s linear
  }

	@media (max-width: 1000px) {
		.bg {
			background: radial-gradient(circle at center 0, rgba(128, 0, 128, 0.267) 5%, rgb(3, 0, 10) 70%);
		}

		.bg-rings {
			top: -50px;
		}

		.bg-rings img {
			width: 80%;
		}

    .bg-icons {
      padding: 0;
			top: 70px;
		}
	}

	@media (max-width: 600px) {
		.bg {
			background: radial-gradient(circle at center 0, rgba(128, 0, 128, 0.267) 1%, rgb(3, 0, 10) 70%);
		}

		.bg-rings {
			top: 0;
		}

		.bg-rings img {
			width: 110%;
		}    

    .bg-icons {
			top: 70px;
		}

		.bg-icons img {
			width: 120%;
      object-fit: contain;
		}
	}
</style>